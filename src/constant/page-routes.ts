export enum PageRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
  THREADS = "/threads",
  THREAD = "/threads/[id]",
  LEADERBOARD = "/leaderboard",
  PROFILE = "/profile",
  NOT_FOUND = "/404",
}
